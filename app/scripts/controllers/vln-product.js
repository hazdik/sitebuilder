//angular.module('pieologyApp')
//    .controller('vlnProductCtrl', ['$scope', 'vlnUpdateManager', function ($scope, vlnUpdateManager) {
//        'use strict';

        // DEV NOTE: I Put this here while exporint how to get started
        // w/ Firebase 3way binding.Commenting it out doesn't seem to
        // break the app. Do you still need it? Justify or remove this
        // during the next housecleaning
//
//        $scope.updatedProduct = {};
//        // $scope.currentProduct = vlnUpdateManager.getFBReference('products/' + product.id);
//
//        $scope.getCurrentProduct = function () {
//            vlnUpdateManager.getFBReference($scope.currentProduct.id).$bind(scope, 'updatedProduct');
//        };
//    }]);